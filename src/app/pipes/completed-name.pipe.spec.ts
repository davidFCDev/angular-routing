import { CompletedNamePipe } from './completed-name.pipe';

describe('CompletedNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedNamePipe();
    expect(pipe).toBeTruthy();
  });
});
